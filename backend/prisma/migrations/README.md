# Prisma migrations

Run these commands from the repository root:

- `npm run db:generate`
- `npm run db:migrate`
- `npm run db:seed`

This folder is intentionally present as a placeholder so migration history can be committed as the schema evolves.
